# Root config: point Netlify to the frontend subdirectory
[build]
  base = "crm_frontend"
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
